# school-timetable-generator-138988-138998

This workspace contains the Random Time Table frontend built in React. Open the `random_time_table_frontend` folder and run:

- npm install
- npm start

Then visit http://localhost:3000 to view the Ocean Professional themed UI.